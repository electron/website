# Needed to resolve API history versions. Without it, the version column of
# every API history table renders as "None". Any token with `actions:read`
# works, e.g. `GITHUB_TOKEN=$(gh auth token)`. In CI this is supplied by
# `secrets.GITHUB_TOKEN`.
GITHUB_TOKEN=
CROWDIN_PERSONAL_TOKEN=
